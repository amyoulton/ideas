<nav class='navbar navbar-expand-lg navbar-light bg-light'>
  <ul class='navbar-nav mr-auto'>
    <li class="nav-item"><%= link_to 'Ideas', ideas_path %> </li>
    <li class="nav-item"><%= link_to 'New Idea', new_idea_path %></li>
 </ul>
  <div>
    <% if user_signed_in? %>
      <%= link_to "Logout", session_path, method: :delete, class: 'btn btn-danger' %>
      <%= current_user.first_name %> <%= current_user.last_name %>
    <% else %>
      <%= link_to "Log In", new_session_path, class: 'btn btn-success'%>
    <% end %>
  </div>
</nav>